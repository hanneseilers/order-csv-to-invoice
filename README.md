# Invoicer (Modular, Klassenbasiert) – HTML → PDF
## Installation
python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
## Nutzung
python -m invoicer.app --csv orders.csv --out out_invoices
